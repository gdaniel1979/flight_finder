name: Deploy to DigitalOcean

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 134.209.226.208
          username: gdaniel1979
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/gdaniel1979/my_projects/flight_finder_ryanair
            git pull